import { ArrowDirectionPipe } from './arrow-direction.pipe';

describe('ArrowDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrowDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
