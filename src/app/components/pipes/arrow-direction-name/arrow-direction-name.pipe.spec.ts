import { ArrowDirectionNamePipe } from './arrow-direction-name.pipe';

describe('ArrowDirectionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrowDirectionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
