import { PrecipitationPipe } from './precipitation.pipe';

describe('PrecipitationPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecipitationPipe();
    expect(pipe).toBeTruthy();
  });
});
